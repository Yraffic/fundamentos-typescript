/* class User {
    name: string = "yuri"
    age: number = 22

    constructor(name: string, age: number){
        this.name = name
        this.age = age
    }

    showName = ()=>{
        console.log(this.name)
    }
}

const user: User = new User("caua", 16)
user.showName() */

/* const soma = (a: number, b: number)=>{
    console.log(a + b)
}

soma(1, 4) */

/* interface Pessoa {
    nome: string,
    idade: number,
    profissao?: string //opcional
}

const pessoa: Pessoa = {
    nome: "yuri",
    idade: 22
}

const aarayPessoa: Array<Pessoa> =[
    pessoa
]

const arrayNumber: number[] =[
    1, 2, 3
]

const arrayString : Array<string> = [
    "a", "b", "c"
] */

/* const typeUser ={
    adim: "admin",
    studant : "studant",
    viewer : "vizualizar"
}

function validateUser(user: string){
    console.log(user as keyof typeof typeUser )
}
 */

